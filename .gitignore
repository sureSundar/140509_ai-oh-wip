# Large files only
*.tar.gz
**/sales_transactions.json

# Only exclude Python cache files
__pycache__/
*.pyc

# Only exclude specific large node_modules if present
**/node_modules/

# Only exclude log files, not log directories with content
*.log